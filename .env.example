# PostgreSQL database URL
# Docker Compose (from this repo):
# DATABASE_URL="postgresql://lvaep:lvaep@localhost:5432/lvaep"
#
# Local Homebrew Postgres (peer/trust auth as your OS user):
DATABASE_URL="postgresql://localhost:5432/lvaep"
